/*********************************************************************************************************
* 模块名称：Measure.h
* 摘    要：信号测量显示模块，负责ADC波形映射显示、频率/统计量展示
* 当前版本：1.0.0
* 作    者：成员C（信号测量模块）
* 完成日期：2026年08月
* 内    容：基础任务6/7——模拟/数字信号波形显示
*           进阶任务1——最大值/最小值/平均电压/峰峰值计算
* 注    意：波形显示调用成员A的OLED画点函数；刷新率≥10Hz
**********************************************************************************************************
* 取代版本：
* 作    者：
* 完成日期：
* 修改内容：
* 修改文件：
*********************************************************************************************************/
#ifndef _MEASURE_H_
#define _MEASURE_H_

/*********************************************************************************************************
*                                              包含头文件
*********************************************************************************************************/
#include "DataType.h"
#include "System.h"

/*********************************************************************************************************
*                                              宏定义
*********************************************************************************************************/
#define MEASURE_WAVE_POINTS  128   //波形显示采样点数
#define MEASURE_REFRESH_MS   100   //波形刷新周期100ms(10Hz)

/*********************************************************************************************************
*                                              枚举结构体定义
*********************************************************************************************************/

/*********************************************************************************************************
*                                              API函数声明
*********************************************************************************************************/
void    InitMeasure(void);              //初始化测量显示模块
void    Measure_DisplayWave(void);      //在OLED上绘制ADC波形
void    Measure_UpdateStats(void);      //更新ADC统计量(进阶)
float   Measure_GetVoltage(void);       //获取当前电压值(V)
u16*    Measure_GetADCBuffer(void);     //获取ADC采样缓冲区指针
u32     Measure_GetFreq(void);          //获取频率测量值(Hz)
u8      Measure_HasSignal(void);        //是否有有效信号
void    Measure_ShowFreqResult(void);   //显示频率测量结果(含无信号提示)

#endif
